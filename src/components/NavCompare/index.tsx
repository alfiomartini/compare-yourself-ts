import React from "react";
import { Button } from "../Button";

export const NavCompare = () => {
  return (
    <div>
      <Button>Add</Button>
      <Button>Get</Button>
      <Button>Delete</Button>
      <Button>List</Button>t
    </div>
  );
};
